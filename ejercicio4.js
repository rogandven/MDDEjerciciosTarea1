"use strict";

// Crea un array con 5 frutas y recórrelo con un bucle for, mostrando cada fruta en consola

const frutas = ["Manzana", "Plátano", "Pera", "Uva", "Naranja"];

for (let i = 0; i < frutas.length; i += 1) {
    console.log(frutas[i]);
}